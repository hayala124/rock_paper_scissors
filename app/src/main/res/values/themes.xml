<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Rock_paper_scissors" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Rock_paper_scissors" parent="Base.Theme.Rock_paper_scissors" />

    <style name="style_for_button">
        <item name="android:layout_width">125dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_marginTop">@dimen/margin_top_for_button_2</item>
        <item name="backgroundTint">@color/purple</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="style_for_image">
        <item name="android:layout_width">@dimen/size_image</item>
        <item name="android:layout_height">@dimen/size_image</item>
    </style>

    <style name="style_for_text">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginTop">30dp</item>
        <item name="android:visibility">invisible</item>

    </style>
</resources>